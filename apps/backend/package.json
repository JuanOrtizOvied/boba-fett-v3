{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "concurrently -n graph,api -c blue,green \"yarn dev:graph:lite\" \"yarn dev:api\"",
    "dev:graph": "langgraph up --port 2024 --watch",
    "dev:graph:lite": "langgraph dev --host 0.0.0.0 --port 2024 --no-browser",
    "dev:api": "test -x .venv/bin/uvicorn && .venv/bin/uvicorn api.routes:app --app-dir src --reload --port 3003 --log-level debug || uvicorn api.routes:app --app-dir src --reload --port 3003 --log-level debug",
    "build": "echo 'backend has no build step'",
    "lint": "test -x .venv/bin/ruff && .venv/bin/ruff check src/ || ruff check src/",
    "test": "test -x .venv/bin/pytest && .venv/bin/pytest -q || pytest -q"
  }
}
