{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "langgraph dev --host 0.0.0.0 --port 2024 --no-browser",
    "build": "echo 'backend has no build step'",
    "lint": "ruff check src/"
  }
}
